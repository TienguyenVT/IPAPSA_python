# Core audio processing and mathematics
numpy>=1.21.0
scipy>=1.7.0
numba>=0.58.0

# Real-time audio I/O
PyAudio>=0.2.11

# Audio file reading/writing (used in offline simulation)
soundfile>=0.10.3

# Objective quality metrics
pystoi>=0.3.3

# Note for Windows users regarding PESQ:
# The `pesq` package requires a C compiler to install properly. 
# If you get an error like "Microsoft Visual C++ 14.0 is required",
# you can either:
#   1. Install "Desktop development with C++" from Visual Studio Installer
#   2. Or comment out the pesq line below if you don't need PESQ metrics.
# (The system will gracefully fallback and report "pesq not installed" if it fails)
pesq>=0.0.4
